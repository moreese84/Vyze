<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/loading_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.Vyze.MaterialOverlay"
    android:contentDescription="Vyze loading screen">

    <!-- Animated brand dots background (red + yellow on white) -->
    <com.vyze.app.ui.BrandDotsView
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="32dp">

        <!-- Full Vyze wordmark logo — uses the adaptive icon foreground which
             contains the complete v+y+z+e wordmark with red and yellow dots,
             properly scaled via nested transform groups. -->
        <ImageView
            android:id="@+id/logo_image"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="fitCenter"
            android:contentDescription="@string/app_name"
            android:importantForAccessibility="yes" />

        <!-- Progress bar with percentage — modernized: thicker rounded
             track, label BELOW the bar (the old -4dp overlap hack
             clipped descenders) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="48dp"
            android:orientation="vertical"
            android:clipChildren="false">

            <com.google.android.material.progressindicator.LinearProgressIndicator
                android:id="@+id/progress_bar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:max="100"
                android:progress="0"
                app:trackCornerRadius="8dp"
                app:trackThickness="16dp"
                app:indicatorColor="@color/vyze_yellow"
                app:trackColor="#1A231F20"
                tools:progress="45" />

            <TextView
                android:id="@+id/progress_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="end"
                android:text="0%"
                android:textColor="#231f20"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"
                tools:text="45%" />
        </LinearLayout>

        <!-- Status text — TalkBack announces changes automatically -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="Preparing Vyze..."
            android:textColor="#333333"
            android:textSize="14sp"
            android:gravity="center"
            android:importantForAccessibility="yes"
            android:accessibilityLiveRegion="assertive"
            tools:text="Copying SmolVLM2 model..." />

        <!-- Error state: Retry button — pill-shaped brand button with ripple -->
        <Button
            android:id="@+id/retry_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:minWidth="160dp"
            android:minHeight="48dp"
            android:paddingHorizontal="32dp"
            android:text="Retry"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            android:stateListAnimator="@null"
            android:background="@drawable/bg_retry_button"
            android:visibility="gone"
            android:importantForAccessibility="yes" />

        <!-- ADB push hint -->
        <TextView
            android:id="@+id/hint_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="#666666"
            android:textSize="11sp"
            android:gravity="center"
            android:fontFamily="monospace"
            android:visibility="gone"
            tools:text="adb push SmolVLM2-500M.litertlm /sdcard/Download/" />

    </LinearLayout>

</FrameLayout>
